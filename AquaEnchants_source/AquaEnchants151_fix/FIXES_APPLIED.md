# AquaEnchants162 fix

Внесённые правки:

1. Добавлена единая защита `ProtectedBlockUtil`: `BEDROCK` нельзя ломать/заменять кастомными эффектами.
2. `trench` / `BREAK_BLOCK` больше не добавляет бедрок в список блоков 3x3 и дополнительно проверяет блок перед `setType(AIR)`.
3. `LineMine`, `VeinMiner`, `BreakTree`, `SmeltEffect`, `EffectManager`, `BlockBreakListener` получили защиту от обработки бедрока.
4. Экскаватор теперь принудительно пропускает бедрок даже если его удалят из `items/excavator.yml` `darklist/blacklist`.
5. Убраны лавовые партиклы из переплавки и связанных массовых эффектов; `LavaEffect` оставлен как no-op для совместимости старых конфигов.
6. У `trench` звук срабатывает только на первой ступени, а не на каждом слое/этапе.
7. Из `enachants.yml` удалены вложенные `partikle: LAVA` у `smelting`.

Сборка `.jar` в этой среде не выполнялась: здесь нет Maven и локальных зависимостей из `pom.xml` (`C:/1.21.8/...`).
